apiVersion: apps/v1
kind: Deployment
metadata:
  name: sark-api
spec:
  template:
    spec:
      containers:
        - name: sark-api
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
